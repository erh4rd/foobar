This project is to learn the ins and outs of GitHub

Erhard, Feb '12

1) Setup

	$ get init

	$ touch <file name>

	$ git add <file name>

	$ git commit -m "<your comment>"

	$ git remote add origin git@github.com:erh4rd/foobar.git

	$ git push origin master

1) Changes

	$ touch <file name>

	$ git add <file name>

	$ git commit -m "<your comment>"

	$ git push origin master

